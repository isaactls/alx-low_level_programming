#include<stdio.h>
/**
 * _strlen - show length of string aka array of char
 * @s: varaible of char type
 *
 * return:int
 */

int _strlen(char *s)
{
	int i;

		for (i = 0; s[i] != '\0'; i++)
		{
			continue;
		}
return (i);
}
