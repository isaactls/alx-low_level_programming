#include<stdio.h>
#include "main.h"
/**
 * _strlen - show length of string aka array of char
 * @s: varaible of char type
 *
 * return: non
 */

int _strlen(char *s)
{
char ss[*s];

strlen(ss);
return(ss);
}
