#include<stdio.h>
#include "main.h"
/**
 * _strlen - show length of string aka array of char
 * @s: varaible of char type
 *
 * return: non
 */

int _strlen(char *s)
{
	int i;

		for (i = 0; s[i] != '\0'; i++)
		{
			continue;
		}
return (i);
}
