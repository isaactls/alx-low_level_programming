#include "main.h"
/**
 * print_last_digit - entry poitn
 * @n: variable
 *
 * Return: 0 (seccess)
 */
int print_last_digit(int n)
{
int a,b;
if (a < 0)
a = n % 10;
p = '0' + a;
_putchar(p);
return (1);
}
