#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

char a;
char b;
for (a = 97; a <= 122; a++)
{
putchar(a);
}
putchar(10);
char (b = 65; b <= 90; b++)
{
putchar(b);
}

return (0);
}
